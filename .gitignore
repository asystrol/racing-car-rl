# Godot 4+ specific ignores
.godot/

# Godot 3-specific ignores (just in case you are on an older version)
.import/
export.cfg
export_presets.cfg

# Imported translations (if you generate them from CSV, etc.)
*.translation

# Mono-specific ignores (only matters if you are using C#)
.mono/
data_*/
mono_crash.*.json
sb3/logs/
model3/
